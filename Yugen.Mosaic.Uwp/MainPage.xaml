<Page x:Class="Yugen.Mosaic.Uwp.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Yugen.Mosaic.Uwp"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:imaging="using:Windows.UI.Xaml.Media.Imaging"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel>
            <Image x:Name="masterImage"
                   Height="120"
                   Width="120"
                   Source="{x:Bind ViewModel.MasterImageSource, Mode=OneWay}" />

            <ListView x:Name="tileListView"
                      ItemsSource="{x:Bind ViewModel.TileList, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="imaging:WriteableBitmap">
                        <Image Source="{x:Bind}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <StackPanel Grid.Column="1">
            <Image x:Name="outputImage"
                   Source="{x:Bind ViewModel.OutputImageSource, Mode=OneWay}" 
                   Stretch="None"/>
        </StackPanel>

        <StackPanel Grid.Column="2">

            <TextBox Header="Tile Width"
                     Text="{x:Bind ViewModel.TileWidth, Mode=TwoWay}" />

            <TextBox Header="Tile Height"
                     Text="{x:Bind ViewModel.TileHeight, Mode=TwoWay}" />

            <TextBox Header="Output Width"
                     Text="{x:Bind ViewModel.OutputWidth, Mode=TwoWay}" />

            <TextBox Header="Output Height"
                     Text="{x:Bind ViewModel.OutputHeight, Mode=TwoWay}" />

            <Button Content="Add Master"
                    Click="{x:Bind ViewModel.AddMasterButton_Click}" />

            <Button Content="Add Tiles"
                    Click="{x:Bind ViewModel.AddTilesButton_Click}" />

            <Button Content="Generate"
                    Click="{x:Bind ViewModel.GenerateButton_Click}" />

            <Button Content="Save"
                    Click="{x:Bind ViewModel.SaveButton_Click}" />

            <ProgressRing Height="40"
                          Width="40"
                          IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                          IsEnabled="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />

        </StackPanel>

    </Grid>
</Page>
